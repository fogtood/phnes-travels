import { Check } from "lucide-react";

const CheckIcon = () => {
  return (
    <div className="inline-block rounded-full bg-green-500 p-1 text-white">
      <Check size={16} />
    </div>
  );
};

export default CheckIcon;
